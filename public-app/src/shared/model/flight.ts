export interface Flight {
  id: string;
  ticketPrice: number;
  dateSource: string;
  dateDestination: string;
  placeSource: string;
  placeDestination: string;
  totalTickets: number;
  boughtTickets: number;
}